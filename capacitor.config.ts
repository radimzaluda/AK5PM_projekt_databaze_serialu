import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AK5PM_projekt_databaze_serialu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
